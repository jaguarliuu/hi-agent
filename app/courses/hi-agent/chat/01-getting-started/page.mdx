---
title: 1.1 聊起来先
---

import { Callout } from 'nextra/components'
import { DocLink } from '../../../../lib/doc-link'
import { CodeBlock } from '../../../../lib/code-block'
import { SourceLink } from '../../../../lib/source-link'

# 1.1 聊起来先

<Callout type="info">
从本小节开始，整个 Hi-Agent 的实践便开始了。现在我们会直接使用 **TypeScript + Node.js** 来写示例项目，并把代码放进页面右侧的 WebContainer 里运行。
这样你不需要在本地提前准备复杂环境，打开课程页面就能看到代码、编辑代码、运行代码。
</Callout>

无论一个 Agent 系统看起来多么复杂，它的核心入口始终离不开 Chat。

当然，这里的 Chat 已经不再只是“人类在对话框里发一句话”。在今天的 Agent 系统中，它可以来自用户输入，也可以来自定时任务触发、心跳触发、Webhook 触发，甚至来自另一个 Agent 的调用。

但本质上，这些触发方式并没有区别。

只要你想唤醒一个 Agent，就必须给它一个入口；只要 Agent 要开始工作，就一定需要接收一条“消息”。这条消息可能是一句自然语言，也可能是一段结构化任务，还可能是系统在某个时间点自动生成的指令。

所以，Chat 是 Agent 的起点。

因此，在这一小节里，我们不会一上来就谈复杂的工具调用、多 Agent 协作或者长期记忆，而是先回到最基础、也最关键的地方：

搭建项目框架，准备模型连接器，启动项目，然后让 Agent 真正“聊起来”。

<PlaygroundSection sectionId="chat-01-getting-started">

<OpenProjectButton blockId="open-example">
打开右侧 Playground
</OpenProjectButton>

## 1.1.1 搭建脚手架

<Callout type="default">
这里我默认大家对 TypeScript 和 Node.js 有基本了解。新建项目本身不是重点，重点是看清楚我们把一次最小 Chat 调用拆成了哪些能力：配置、模型连接器、Chat Client、会话状态和命令行入口。
</Callout>

初始化项目的结构如下：

<CodeBlock title="项目初始目录结构">

```bash
.
├── package.json
├── tsconfig.json
├── .env.example
└── src
    ├── config.ts
    ├── model-provider.ts
    ├── chat-message.ts
    ├── chat-client.ts
    ├── chat-session.ts
    └── main.ts
```

</CodeBlock>

接下来就是安装依赖。我们这里引入 Vercel 的 AI SDK，以及 OpenAI 兼容模型供应商适配器：

<CommandBlock blockId="install-deps">

```bash
npm install
```

</CommandBlock>

<CodeBlock title="package.json — 核心依赖" defaultOpen>

```json
{
  "dependencies": {
    "ai": "^5.0.0",
    "@ai-sdk/openai-compatible": "^1.0.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
```

</CodeBlock>

`ai` 是 Vercel AI SDK 的核心包，负责提供 `generateText` 这类统一调用接口。`@ai-sdk/openai-compatible` 则用于接入 OpenAI 兼容协议的模型服务。

从 ChatGPT 火出圈之后，OpenAI 的接口协议逐渐变成了大模型应用开发中的一种事实标准。后续很多大模型厂商都开始兼容 OpenAI API 格式，包括我们常用的国内大模型服务商，比如 Qwen、DeepSeek、MiniMax、GLM 等。

这意味着，只要这些厂商提供了 OpenAI 兼容接口，我们就可以通过配置不同的 base URL、API Key 和模型名，使用同一套代码调用不同厂商的模型服务。

所以，在这一节里，我们选择 AI SDK，并不只是为了调用 OpenAI 官方模型，更重要的是借助它统一不同模型厂商的接入方式。

接下来，我们还需要新建一个配置文件 `.env`，用于存储模型 API Key 和 Base URL。在 WebContainer 里，你可以直接复制 `.env.example` 为 `.env`，再填入自己的配置。

<CodeBlock title=".env.example — DeepSeek 示例配置" defaultOpen>

```bash
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=sk-用自己的API-KEY
OPENAI_MODEL=deepseek-v4-flash
```

</CodeBlock>

如果你使用 Qwen，也可以换成下面这组 OpenAI 兼容配置：

<CodeBlock title=".env — Qwen 示例配置" defaultOpen>

```bash
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=sk-你的百炼APIKey
OPENAI_MODEL=qwen-plus
```

</CodeBlock>

<Callout type="warning">
这里的 `.env` 只存在于你当前浏览器里的 WebContainer 文件系统中。它不会上传到本站服务器，不会被课程站点记录，也不会被我们存储。刷新页面、重置 Playground 或清理浏览器数据后，这些配置可能会消失，所以请只把它当作本地临时运行环境。
</Callout>

<Callout type="error">
如果你把示例代码复制到自己的 GitHub 仓库里，请一定记得把 `.env` 加入 `.gitignore`，否则你的 API Key 会被泄露出去。
</Callout>

## 1.1.2 实现配置解析和聊起来

接下来，我们想和 AI 聊起来，其实特别简单。和大模型的交互，本质上还是请求一个远端 API，和我们日常访问其他服务没有本质差异。

因此，流程上和 HTTP 请求也没有特别大的差异：读取配置，创建模型连接器，构造消息，发起请求，最后解析响应即可。

### 1.1.2.1 OpenAiConfig 配置解析

我们先来解析 `.env`，从环境变量中获取 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL` 等参数。

<RunnableCodeBlock blockId="config-snippet">

```ts filename="src/config.ts"
import 'dotenv/config';

export interface OpenAiConfig {
  baseUrl: string;
  apiKey: string;
  model: string;
}

export function loadConfig(): OpenAiConfig {
  return {
    baseUrl: readRequiredEnv('OPENAI_BASE_URL'),
    apiKey: readRequiredEnv('OPENAI_API_KEY'),
    model: readRequiredEnv('OPENAI_MODEL')
  };
}

function readRequiredEnv(key: string): string {
  const value = process.env[key]?.trim();
  if (!value) {
    throw new Error(
      `Missing required config: ${key}. 请在 .env 中配置你的模型连接信息。`
    );
  }
  return value;
}
```

</RunnableCodeBlock>

这里的 `OpenAiConfig` 本质上只是一个只读的数据载体，没有业务状态变化。它只有三个字段：`baseUrl`、`apiKey`、`model`。

具体实现上，我们通过 `dotenv` 把 `.env` 加载到 `process.env`，再逐个读取 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`。如果某个配置缺失，就抛出明确的错误，比如 `Missing required config: OPENAI_MODEL`。

这类错误越早暴露越好。否则等到真正请求模型时，你可能只会看到认证失败、网络失败，或者一个很模糊的 SDK 报错。

### 1.1.2.2 创建模型连接器

接下来我们把 AI SDK 的 OpenAI 兼容适配器单独封装起来。

<RunnableCodeBlock blockId="provider-snippet">

```ts filename="src/model-provider.ts"
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';
import type { LanguageModel } from 'ai';
import type { OpenAiConfig } from './config.js';

export function createModel(config: OpenAiConfig): LanguageModel {
  const provider = createOpenAICompatible({
    name: 'openai-compatible',
    apiKey: config.apiKey,
    baseURL: config.baseUrl
  });

  return provider(config.model);
}
```

</RunnableCodeBlock>

这一层很薄，但很重要。业务代码不应该到处关心 `baseURL`、`apiKey`、供应商名称这些细节。后面如果我们想切换 DeepSeek、Qwen 或其他 OpenAI 兼容服务，只需要改配置，不需要改 Chat 逻辑。

### 1.1.2.3 OpenAiChatClient 聊起来

有了模型连接器之后，我们就可以封装真正执行 AI 调用的 `OpenAiChatClient`。

<RunnableCodeBlock blockId="client-snippet">

```ts filename="src/chat-client.ts"
import { generateText } from 'ai';
import type { LanguageModel } from 'ai';
import { userMessage, type ChatMessage } from './chat-message.js';

export class OpenAiChatClient {
  constructor(private readonly model: LanguageModel) {}

  async chat(userPrompt: string): Promise<string> {
    return this.chatMessages([userMessage(userPrompt)]);
  }

  async chatMessages(messages: ChatMessage[]): Promise<string> {
    if (messages.length === 0) {
      throw new Error('messages must not be empty');
    }

    const result = await generateText({
      model: this.model,
      messages
    });

    const reply = result.text.trim();
    if (!reply) {
      throw new Error('AI response did not contain assistant content');
    }

    return reply;
  }
}
```

</RunnableCodeBlock>

`OpenAiChatClient` 是真正执行 AI 调用的类。它内部只保留一个状态：当前模型实例 `model`。

这里没有把配置读取和模型创建逻辑写死在构造器里，而是让外部把 `model` 传进来。这样做的原因和原来一样：可测试、可替换，也让每一层的职责更清楚。

`chat(userPrompt)` 是单轮接口，内部只是把用户输入包装成一条 `user` 消息，再复用 `chatMessages(messages)`。这样逻辑只有一份，不会出现单轮和多轮两套不同实现。

<CodeBlock title="ChatMessage — 内部消息模型" defaultOpen>

```ts
export type ChatRole = 'user' | 'assistant';

export interface ChatMessage {
  role: ChatRole;
  content: string;
}
```

</CodeBlock>

这里我们暂时只保留两个角色：

- `user`
- `assistant`

之所以只保留这两个角色，是因为当前目标只是“命令行多轮对话”的最小版本。先做最少模型，比一开始就把 `system`、`tool` 等角色全部引入更清楚。

### 1.1.2.4 聊起来

现在我们已经可以发起一次最小 Chat 调用了。示例项目里最终会从 `main.ts` 启动，但你可以先把它理解成下面这段流程：

<CodeBlock title="单轮 Chat 调用流程" defaultOpen>

```ts
const config = loadConfig();
const model = createModel(config);
const client = new OpenAiChatClient(model);

const reply = await client.chat('你好呀～');
console.log(reply);
```

</CodeBlock>

一切顺利的话，你会看到模型返回一段正常的助手回复。

但现在还有一个问题：如果我们每次都在代码里改提示词，就不像真正聊天。我们需要让它支持对话，哪怕只是命令行里输入一句话，它也能返回一个回复，并且可以继续接着聊。

## 1.1.3 支持对话

我们要实现的对话部分也没有那么复杂。当 `main.ts` 启动后，我们就开启一个循环，等待用户输入。当用户输入之后，就调用 `OpenAiChatSession`，获取回复，最后打印回复。

直到用户输入 `exit`，或者停止进程，我们才退出循环。

当然在这里，我们还需要简单处理上下文。既然是多轮对话，那么我们就需要将完整的历史会话传递给模型，让模型可以持续在同一个上下文里和我们交互。你甚至可以简单地理解为，这就是 Context Engineering 的雏形。

### 1.1.3.1 引入 `OpenAiChatSession` 管理会话状态

多轮对话的本质不是“循环读取输入”，而是“每一轮请求都带上下文”。

所以我们新增一个会话管理类 `OpenAiChatSession`。

<RunnableCodeBlock blockId="session-snippet">

```ts filename="src/chat-session.ts"
import {
  assistantMessage,
  userMessage,
  type ChatMessage
} from './chat-message.js';
import type { OpenAiChatClient } from './chat-client.js';

export class OpenAiChatSession {
  private readonly history: ChatMessage[] = [];

  constructor(private readonly client: OpenAiChatClient) {}

  async send(userInput: string): Promise<string> {
    const currentUserMessage = userMessage(userInput);
    const requestMessages = [...this.history, currentUserMessage];

    const reply = await this.client.chatMessages(requestMessages);
    this.history.push(currentUserMessage, assistantMessage(reply));

    return reply;
  }

  getHistory(): readonly ChatMessage[] {
    return [...this.history];
  }
}
```

</RunnableCodeBlock>

`OpenAiChatSession` 内部只保留了：

- `OpenAiChatClient client`
- `ChatMessage[] history`

对外只暴露一个核心方法：

<CodeBlock title="send — 会话对外接口" defaultOpen>

```ts
async send(userInput: string): Promise<string>
```

</CodeBlock>

这个方法的流程很简单：

1. 把当前用户输入包装成 `user` 消息
2. 基于旧历史复制出一份临时请求列表
3. 把当前用户消息加到临时请求列表末尾
4. 调用 `client.chatMessages(requestMessages)`
5. 调用成功后，再把 `user` 和 `assistant` 两条消息写回正式历史

“成功后再写历史”是这一层最关键的设计点。如果一开始就把用户消息写进正式历史，一旦请求失败，就会留下这种不一致状态：

- 用户已经说了一句话
- assistant 没有回应

这会让下一轮上下文变得奇怪，也会让调试变复杂。

所以这里采用更稳妥的策略：先构造临时请求，调用成功之后，再把这一轮 user 和 assistant 一起提交到历史中。

### 1.1.3.2 最后把能力接到 `main.ts`

`main.ts` 只负责交互流程：

- 读取配置
- 创建模型连接器
- 创建 `OpenAiChatClient`
- 创建 `OpenAiChatSession`
- 打印启动提示
- 循环读取一行输入
- 空行直接跳过
- 输入 `exit` 时退出
- 其余输入交给 `session.send(...)`
- 打印 AI 回复
- 如果本轮请求失败，只打印错误，不直接杀掉整个进程

<RunnableCodeBlock blockId="main-snippet">

```ts filename="src/main.ts"
import { createInterface } from 'node:readline/promises';
import { stdin as input, stdout as output } from 'node:process';
import { loadConfig } from './config.js';
import { createModel } from './model-provider.js';
import { OpenAiChatClient } from './chat-client.js';
import { OpenAiChatSession } from './chat-session.js';

async function main() {
  const config = loadConfig();
  const model = createModel(config);
  const client = new OpenAiChatClient(model);
  const session = new OpenAiChatSession(client);
  const terminal = createInterface({ input, output });

  console.log('Chat started. Type "exit" to quit.');

  try {
    while (true) {
      const line = await terminal.question('You: ');
      const userInput = line.trim();

      if (!userInput) continue;
      if (userInput === 'exit') break;

      try {
        const reply = await session.send(userInput);
        console.log(`AI: ${reply}`);
      } catch (error) {
        console.error(`AI error: ${formatError(error)}`);
      }
    }
  } finally {
    terminal.close();
  }
}
```

</RunnableCodeBlock>

这里刻意保持 `main.ts` 很薄，不让它保存历史，也不让它自己拼模型请求。这样入口层只做“交互编排”，不会和会话逻辑耦在一起。

配置好 `.env` 之后，就可以运行：

<CommandBlock blockId="run-demo">

```bash
npm run chat
```

</CommandBlock>

实际运行时大概会像这样：

<CodeBlock title="实际运行对话示例" defaultOpen>

```text
Chat started. Type "exit" to quit.
You: 你好，我是 jaguar，你介绍下你自己
AI: 你好，Jaguar！很高兴认识你。
...
You: 你还记得我是谁吗
AI: 当然记得，你刚才告诉我你是 jaguar。
You: exit
```

</CodeBlock>

## 1.1.4 总结

到这里，我们已经完成了 Hi-Agent 的第一步：让项目真正聊起来。

这一小节我们没有一上来就做复杂的 Agent Loop，也没有急着接入 Tool、Memory 或 Multi-Agent，而是先把最基础的 Chat 入口搭了起来。因为对任何 Agent 来说，能被唤醒、能接收消息、能返回结果，都是后续一切能力的前提。

在代码层面，我们完成了几件事：

- 首先，用 `.env` 管理模型配置，把 `OPENAI_BASE_URL`、`OPENAI_API_KEY` 和 `OPENAI_MODEL` 从代码中拆出来，避免把敏感信息写死在程序里。
- 然后，封装了 `OpenAiConfig`，专门负责配置读取和校验。这样一旦配置缺失，程序可以在启动阶段直接报出明确错误。
- 接着，通过 `createModel` 把 AI SDK 的 OpenAI 兼容适配器封装起来，让 DeepSeek、Qwen 这类模型供应商可以通过配置切换。
- 在此基础上，我们实现了 `OpenAiChatClient`，把和模型交互的逻辑集中封装起来。
- 最后，通过 `OpenAiChatSession` 管理会话历史，再把它接入 `main.ts` 命令行入口，实现了一个最小可用的命令行聊天程序。

虽然这个版本还非常简单，但它已经具备了一个 Agent 系统最早期的雏形：

* 有消息入口；
* 有模型客户端；
* 有配置管理；
* 有多轮历史；
* 有最基础的上下文传递；
* 有命令行交互；
* 也有简单的异常处理。

更重要的是，我们已经开始触碰到 Agent 工程里一个非常关键的问题：模型并不会天然记住一切，多轮对话依赖的是我们主动把历史消息重新传给它。

> 这就是 Context Engineering 最早的影子。

当前版本的上下文管理还很粗糙，只是简单地把完整历史消息全部带上。它可以帮助模型在当前会话里记住“我是谁”“刚才说过什么”，但它也有明显问题：历史越长，请求越重；无关信息越多，模型越容易分心；一旦上下文超过模型限制，还需要压缩、裁剪和摘要。

这些问题，我们后面会一步一步解决。

但现在，先别急。

这一节的目标已经完成：我们搭好了项目骨架，也让 Agent 开口说话了。

接下来，我们就可以在这个最小 Chat 系统的基础上，继续拆解 Agent 的核心概念，逐步把它从一个“能聊天的程序”，推进成一个真正“能做事的 Agent”。

<SourceLink
  type="repo"
  repo="docs"
  title="本节 TypeScript 示例项目"
  description="本节示例位于 examples/hi-agent/chat/01-getting-started/workspace，使用 TypeScript、Node.js 和 Vercel AI SDK 接入 OpenAI 兼容模型。"
/>

---

**下一节** → <DocLink href="/courses/hi-agent/chat/02-core-concepts">1.2 核心概念</DocLink>

</PlaygroundSection>

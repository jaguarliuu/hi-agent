---
title: Chat
description: 从对话接口出发，理解 Agent 的最小表达单元。
---

import { Callout, Steps } from 'nextra/components'

# M01 · Chat

> 在我们谈论"Agent"之前，先回到一切的起点：**Chat**。

## 本章目标

- 理解 Chat Completion 接口的基本构成：messages、roles、streaming
- 认识"对话"作为 Agent 输入/输出协议的本质
- 掌握一个最小可运行的对话循环

<Callout type="info">
本节内容占位中。你将学到：消息结构、角色体系、流式输出、Token 与上下文窗口、多轮对话的工程化。
</Callout>

## 章节结构

<Steps>

### 消息与角色

`system` / `user` / `assistant` / `tool`——四种角色的设计哲学与使用边界。

### 流式输出

为什么所有现代 Agent 都必须支持 streaming？背压、取消、增量渲染。

### 上下文窗口

Token 是一种稀缺资源。本节讨论窗口管理的基础策略。

### 多轮对话

一个最小的对话循环是如何工作的，以及它离"Agent"还差什么。

</Steps>

## 一个最小示例

```ts
// 占位：后续补充一个最小可跑的 Chat 示例
import OpenAI from 'openai'

const client = new OpenAI()

const stream = await client.chat.completions.create({
  model: 'gpt-4o-mini',
  stream: true,
  messages: [
    { role: 'system', content: 'You are a helpful assistant.' },
    { role: 'user', content: 'Hello!' }
  ]
})

for await (const chunk of stream) {
  process.stdout.write(chunk.choices[0]?.delta?.content ?? '')
}
```

---

**下一章** → [M02 · Agent Loop](/docs/agent-loop)

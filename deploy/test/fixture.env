# Test fixture for compose config parsing only.
# 不用于真实 docker compose up；仅满足 ${VAR:?} 校验。
HI_AGENT_IMAGE=test/hi-agent:fixture
AUTH_SERVER_IMAGE=test/auth-server:fixture
POSTGRES_DB=hi_agent
POSTGRES_USER=hi_agent
POSTGRES_PASSWORD=fixture-password
DATABASE_URL=postgresql://hi_agent:fixture-password@postgres:5432/hi_agent?schema=public
OTP_PEPPER=fixture-pepper
SESSION_TTL_DAYS=30
SECURE_COOKIE=false
TRUST_PROXY=true
SMTP_HOST=fixture.smtp
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=fixture
SMTP_PASS=fixture
SMTP_FROM=fixture@example.com
APP_BASE_URL=http://127.0.0.1:8080
